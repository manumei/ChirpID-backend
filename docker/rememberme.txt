Refactoricemos los components de docker aca (si no es mucho quilombo cambiar los paths y eso)
(Aun ni se hizo el deploy anyway, o no?)

Respuesta: Se puede, pero hay archivos que deben estar en el root (docker-compose.yml, nginx.conf)

Si pasamos deploy.sh a esta misma carpeta habra algun problema? Llama otros archivos? 
Le decimos a copilot que ajuste los paths y listo. O los ajustamos a mano, tampoco deben ser tantos.

Respuesta: Desconozco.